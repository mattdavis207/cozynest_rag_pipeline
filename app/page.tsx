export default function HomePage() {
  // TODO: Decide what the landing page should show once the backend flow exists.
  // Keep this page intentionally plain while learning the RAG pipeline.
  return (
    <main>
      <h1 className="text-2xl font-semibold">CozyNest RAG Pipeline</h1>
      <p className="mt-2 text-muted-foreground">
        TODO: Build the backend/RAG workflow before adding a real interface.
      </p>
    </main>
  );
}
